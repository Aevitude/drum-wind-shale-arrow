---
name: silk-dossier
description: 用仓库里做好的高级简历 HTML，先问用户四件事，润色后只替换标注好的文字，改一行气质，生成网页应用卡片。禁止重写页面、禁止开服务器。触发词：简历、履历、dossier、用这个 skill。
---

# 履历一页（豆包专用）

仓库里的 `public/resume.html` 已经是成品。你不是来设计简历的。

你只做：问四句 → 润色 → 替换标注文字 → 改 body 的 theme → 生成网页应用卡片。然后停。

## 1. 先问，一次问完

> 做一页履历。回我这四项就好，没有的写「无」。
>
> 1. 姓名
> 2. 你做什么（几个字，例如：空间与物件）
> 3. 三件最能拿得出手的事（各用一句话）
> 4. 城市，以及微信或邮箱

不要再问颜色、排版、照片、教育经历。

问完后让用户选气质（只选一个）：

> 1. 象牙  2. 墨夜  3. 陶土  4. 苔绿  5. 沙色
> 6. 酒红  7. 石灰  8. 夜香槟  9. 瓷青  10. 锈橙

不选就用象牙。

## 2. 只改 resume.html 里的字

打开 `public/resume.html`。

- `<body class="theme-ivory">` 按表改 theme 名
- 只替换注释中间的字，注释本身不要删

| 气质 | class |
|---|---|
| 象牙 | `theme-ivory` |
| 墨夜 | `theme-ink` |
| 陶土 | `theme-clay` |
| 苔绿 | `theme-moss` |
| 沙色 | `theme-sand` |
| 酒红 | `theme-wine` |
| 石灰 | `theme-stone` |
| 夜香槟 | `theme-night` |
| 瓷青 | `theme-porcelain` |
| 锈橙 | `theme-rust` |

替换这些槽：

- `<!--NAME-->` 姓名
- `<!--ROLE-->` 身份，短
- `<!--CITY-->` 和 `<!--CITY2-->` 同一座城市
- `<!--STATEMENT-->` 你根据用户的三件事，写成 **两到三句** 的简介，语气冷静，不要鸡汤，不要感叹号
- `<!--W1T-->` `<!--W1B-->` 第一件事的短标题 + 一句说明
- `<!--W2T-->` `<!--W2B-->` 第二件
- `<!--W3T-->` `<!--W3B-->` 第三件
- `<!--CONTACT-->` 微信或邮箱；没有就写城市

同时改 `<title>` 为姓名。

## 3. 生成卡片，立刻停

把改好的 **这一个 HTML 文件** 做成豆包的网页应用。左边出现小卡片、右边能看见履历，就结束。

跟用户说：点下面的卡片就能看。

### 绝对禁止

- 不要重写 CSS、不要加 React、不要 npm、不要 localhost
- 不要把文件打成 JS 包
- 不要加照片、进度条、技能圆点、蓝紫渐变
- 不要删注释
- 不要说这是模板

写错 theme 就留 `theme-ivory`。
